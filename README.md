# Text Summarizer & Paraphraser

## Setup
1. Create virtual env:
   python -m venv .venv
   .venv\Scripts\Activate.ps1

2. Install:
   pip install -r requirements.txt

3. Copy .env.example to .env and add HF_API_KEY.

4. Run tests:
   python test_run.py

5. Run app:
   streamlit run app.py
